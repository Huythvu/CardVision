__pycache__/
*.pyc
.venv/
venv/
crops/
# Calibrated templates are source-specific; ignore the generated glyphs but
# keep the directory (templates/.gitkeep). Remove these lines if you want to
# commit a template set.
templates/ranks/
templates/suits/

# Model weights and YOLO training runs (large, generated).
models/*.pt
models/*.onnx
runs/
datasets/
